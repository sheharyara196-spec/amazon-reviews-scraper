{
  "schemaVersion": 1,
  "title": "Amazon Reviews Scraper",
  "description": "Scrape and filter Amazon product reviews.",
  "type": "object",
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of Amazon product or review page URLs to scrape",
      "editor": "requestListSources"
    },
    "asin": {
      "title": "ASIN",
      "type": "string",
      "editor": "input",
      "description": "Amazon Standard Identification Number (ASIN) of the product"
    },
    "maxReviews": {
      "title": "Maximum number of reviews",
      "type": "integer",
      "default": 30,
      "minimum": 1,
      "description": "The maximum number of reviews to scrape"
    },
    "stars": {
      "title": "Filter by Star Rating",
      "type": "string",
      "enum": ["all", "one_star", "two_star", "three_star", "four_star", "five_star"],
      "default": "all",
      "description": "Choose which reviews to scrape based on star rating"
    },
    "sortBy": {
      "title": "Sort Reviews",
      "type": "string",
      "enum": ["recent", "top_rated", "helpful"],
      "default": "recent",
      "description": "Sort reviews by newest, top rated, or most helpful"
    },
    "reviewerType": {
      "title": "Reviewer Type",
      "type": "string",
      "enum": ["all_reviews", "verified_purchase"],
      "default": "all_reviews",
      "description": "Filter by all reviews or only verified purchases"
    },
    "mediaType": {
      "title": "Media Type",
      "type": "string",
      "enum": ["all_contents", "with_images", "with_videos"],
      "default": "all_contents",
      "description": "Filter reviews by content type (all, images, or videos)"
    }
  },
  "required": ["startUrls"]
}

